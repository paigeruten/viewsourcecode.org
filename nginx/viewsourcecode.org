server {
        listen 80;
        listen [::]:80;

        root /var/www/viewsourcecode.org;
        index index.html;

        server_name viewsourcecode.org;

        location / {
                try_files $uri $uri/ =404;
        }

        location /code {
                autoindex on;
        }
}
